spring:
  datasource:
    postgres:
      url:
      driverClassName: org.postgresql.Driver
      username:
      password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect